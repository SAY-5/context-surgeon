# Personal notes

My local Postgres runs on port 5433 (5432 is taken by another project).
Override with `DATABASE_URL=postgres://localhost:5433/ledger_dev`.

I keep a scratch notebook at `scratch/` that is gitignored. Feel free to
write exploratory code there.

I prefer ripgrep over grep. `rg` is installed.
